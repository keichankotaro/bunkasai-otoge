# Bunkasai 2023 Otoge 内部仕様・計算式

## 理論値100万点に基づくスコアシステム

譜面上の全ノーツを `Perfect+` で処理した場合のスコアを理論値（$S_{max}$）とし、常に1,000,000点満点となるよう正規化する。

基礎スコア $S_{base}$ は以下の通り。
* Perfect+: 100
* Perfect: 90
* Great: 50
* Good: 10
* Miss: 0

総ノーツ数 $N$ は、タップを1、ロングを始点と終点の2として扱う。
$$ N = N_{tap} + 2 \times N_{long} $$
$$ S_{max} = N \times 100 $$

表示スコア $S_{ratio}$ は、小数の丸め誤差を排除するため床関数を用いて算出する。最大値は1,000,000に制限する。
$$ S_{ratio} = \min\left( 1000000, \mathrm{Round} \left( \frac{ \lfloor \frac{\sum S_{base}}{S_{max}} \times 10^6 \rfloor \times 10^6 }{10^6} \right) \right) $$

**【計算例】**
タップノーツ500個、ロングノーツ50個の譜面の場合、総ノーツ数 $N = 500 + 50 \times 2 = 600$、理論値 $S_{max} = 60,000$ となる。
プレイ結果が Perfect+ 590回、Perfect 8回、Great 2回の場合、獲得スコアの合計は $590 \times 100 + 8 \times 90 + 2 \times 50 = 59,820$ となる。これを式に当てはめると、表示スコアは $997,000$ 点としてUIに出力される。

## 到達時刻との差分による5段階の判定ウィンドウ

ノーツが判定ラインに到達する理論時刻 $T_{arr}$ と、実際のタップ時刻 $T_{tap}$ との差分 $\Delta t = |T_{tap} - T_{arr}|$ によって判定を決定する。

* Perfect+: $\Delta t \le 0.03$
* Perfect: $0.03 < \Delta t \le 0.05$
* Great: $0.05 < \Delta t \le 0.07$
* Good: $0.07 < \Delta t \le 0.10$
* Miss: $\Delta t > 0.10$（未入力のまま $0.18$ 秒経過した場合も含む）

基準となるゲーム内時間 $T_{game}$ は、BGMの再生時間・譜面オフセット・ユーザー設定のキャリブレーション値を加算して求める。
$$ T_{game} = T_{audio} - \frac{O_{chart}}{f_{audio}} + O_{user} $$

**【計算例】**
理論到達時間 $T_{arr} = 12.50$ 秒のノーツを、ユーザーが $T_{tap} = 12.54$ 秒にタップした場合、差分 $\Delta t = 0.04$ 秒となる。これは $0.03 < 0.04 \le 0.05$ の区間に収まるため、内部的には「Perfect」として処理される。

## スコアと判定数に基づくクリア・ランクの評価

アチーブメントは、各判定の取得状況と表示スコアによって評価する。
* **AP+**: $S_{ratio} \ge 1,000,000$
* **AP**: Perfect+ と Perfect の合計が総ノーツ数と等しい
* **FC**: Miss が 0 であり、全ノーツを処理している
* **Clear**: $S_{ratio} \ge 750,000$

ランクは表示スコアの区間で以下のように決定する。
* **SSS+** : 990,000 超
* **SSS**  : 980,000 超 〜 990,000
* **SS+**  : 975,000 超 〜 980,000
* **SS**   : 950,000 超 〜 975,000
* **S+**   : 925,000 超 〜 950,000
* **S**    : 900,000 超 〜 925,000
* **AAA**  : 850,000 超 〜 900,000
* **AA**   : 800,000 超 〜 850,000
* **A**    : 750,000 超 〜 800,000
* **BBB**  : 700,000 超 〜 750,000
* **BB**   : 650,000 超 〜 700,000
* **B**    : 600,000 超 〜 650,000
* **C**    : 550,000 超 〜 600,000
* **D**    : 550,000 以下

**【計算例】**
先ほどの表示スコア 997,000 点のリザルトは、990,000を超えているためランク「SSS+」が付与される。また、Missが0回であるため、同時にアチーブメント「FC」の条件を満たしてバッジが点灯する。

## Vector3を用いたノーツの3D空間座標とスケール変換

3D空間上におけるノーツのベクトル座標 $\bm{P}_{note}$ およびスケール $\bm{S}_{note}$ は、判定ラインの位置、到達予定時間までの残り時間 $T_{rem} = -(T_{game} - T_{arr})$、現在のスクロール速度 $V$ から動的に計算される。

X座標は配置されるレーン番号（L1〜L4）により固定される。レーン同士の干渉を防ぐため、Y座標には微小なオフセットを持たせる。Z座標の基準となる判定ラインの位置を $Z_0 = -10.08565$ とするとき、位置ベクトル $\bm{P}_{note}$ は次式で表される。

$$
\bm{P}_{note} = 
\begin{pmatrix}
x_{lane} \\
y_{offset} \\
\frac{V}{2} T_{rem} + Z_0 + Z_{shift}
\end{pmatrix}
$$

ただし、$y_{offset}$ はロングノーツで $0.01$、タップノーツで $0.02$ とする。$Z_{shift}$ は、ロングノーツの中間描画を行う際の補正値（長尺の半分 $L/2$）であり、タップノーツでは $0$ である。

ロングノーツの物理的長尺 $L$ は、始点時刻と終点時刻の時間差分に基準速度 $V_{base}$ を掛けて展開する。スケールベクトル $\bm{S}_{note}$ は以下となる。
$$ L = \frac{V_{base} \times (T_{end} - T_{start})}{2} $$
$$
\bm{S}_{note} = 
\begin{pmatrix}
0.8 \\
0.01 \\
L
\end{pmatrix}
$$
タップノーツの場合は $L = 0.1$ の固定値を取る。

**【計算例】**
スクロール速度 $V = 10.0$ で、ノーツ到達までの残り時間 $T_{rem} = 2.0$ 秒の時点において、ノーツのZ座標は $\frac{10.0}{2} \times 2.0 - 10.08565 = -0.08565$ の位置に描画される。
また、基準速度 $V_{base} = 10.0$、始点と終点の時間差 $0.5$ 秒のロングノーツの場合、Z軸のスケール（長尺 $L$）は $10.0 \times 0.5 / 2 = 2.5$ となる。

## 三次ベジェ曲線を用いたソフラン（速度変化）

曲中の速度変化（ソフラン）は、開始時刻 $T_s$ から期間 $D$ をかけて、現在の倍率 $M_{start}$ から目標倍率 $M_{target}$ へと遷移させる。この遷移の補間係数 $\alpha$ の算出に三次ベジェ曲線を用いる。

正規化時間 $t = (T_{game} - T_s) / D$ に対し、制御点 $(0,0), (x_1, y_1), (x_2, y_2), (1,1)$ で定義される三次ベジェ曲線 $B(u)$ を用いて $B_x(u^*) = t$ を満たす $u^*$ を二分探索で求め、$\alpha = B_y(u^*)$ とする。

最終的なスクロール速度 $V$ は、基準速度 $V_{default}$ に補間済みの倍率を掛けて求める。
$$ M_{current} = (1 - \alpha) M_{start} + \alpha M_{target} $$
$$ V = V_{default} \times M_{current} $$

**【計算例】**
開始時刻から $D = 2.0$ 秒かけて速度を $1.0$ 倍から $2.0$ 倍に引き上げる設定（JSONの `formula` 指定）において、$t = 0.5$（1秒経過時点）での補間係数 $\alpha$ がベジェ曲線から $0.3$ と導出されたとする。この瞬間の倍率は $(1 - 0.3) \times 1.0 + 0.3 \times 2.0 = 1.3$ 倍となり、ノーツ速度に即座に反映される。

## UIアニメーションとカメラ制御の数理モデル

オブジェクトの滑らかな補間や、カメラの揺れ・回転には、以下の数学関数が直接組み込まれている。

### イージング関数群 (Easing)
アニメーションの正規化時間 $x \in [0,1]$ に対し、31種類にも及ぶ非線形補間関数が定義されている。代表的な式の定義は以下の通り。

* **Sine (三角関数系)**
  * EaseIn: $f(x) = 1 - \cos\left(\frac{\pi x}{2}\right)$
  * EaseOut: $f(x) = \sin\left(\frac{\pi x}{2}\right)$
* **Cubic (多項式系)**
  * EaseIn: $f(x) = x^3$
  * EaseOut: $f(x) = 1 - (1 - x)^3$
* **Circ (円関数系)**
  * EaseIn: $f(x) = 1 - \sqrt{1 - x^2}$
  * EaseOut: $f(x) = \sqrt{1 - (x - 1)^2}$

**【適用例】**
これらの関数は `DOTween` と結合され、楽曲プレビュー開始時の音量フェードインや、リザルト画面のUIスライドインなど、各種オブジェクトのトランジションを滑らかにするために用いられる。

### 単振動によるカメラバウンス (Camera Bounce)
プレイ中のカメラの周期的な揺れは、経過時間 $t$ を変数とする正弦波（単振動）を用いて基準Y座標 $Y_0$ に加算される。角周波数を $\omega = 10$、振幅を $A = 0.2$ とする。
$$ Y(t) = Y_0 + A \sin(\omega t) $$

### 球面座標系に基づくカメラ回転 (Camera Rotation)
カメラのオイラー角制御やベクトル算出において、入力された回転パラメータ $\theta$ を用いた三角関数による座標変換が用いられる。
$$ R_x = R_{base} \cos\left(\frac{\pi}{360} \theta\right) $$
$$ R_y = R_{base} \sin\left(\frac{\pi}{360} \theta\right) $$
*(※ $R_{base}$ は基準となる角度・スケール係数)*

**【計算例】**
譜面の `cameradata` において回転パラメータとして `45`（度）が指定された場合、X軸成分は $47.11 \times \cos(\frac{\pi}{8}) \approx 43.52$ のように算出され、カメラの回転ベクトルへと適用される。
